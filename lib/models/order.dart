class Order{
  final isCarryBag;
  final isCartonBox;
  final oid;
  final orderList;
  final orderType;
  final paidTime;
  final placedTime;
  final shopComment;
  final sid;
  final status;
  final totalCost;
  final uid;
  final userComment;
  final userPickupTime;
  final rating;

  Order({this.isCarryBag, this.isCartonBox, this.oid, this.orderList,
      this.orderType, this.paidTime, this.placedTime, this.shopComment,
      this.sid, this.status, this.totalCost, this.uid, this.userComment,
      this.userPickupTime, this.rating});
}